require "spec_helper"

describe ContainersManager do
  it "has a version number" do
    expect(ContainersManager::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
